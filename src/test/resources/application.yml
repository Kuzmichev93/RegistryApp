
spring:
  jpa:
    database: H2
    show-sql: true
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
      default_schema: registry
  datasource:
    platform: H2
    url: jdbc:h2:mem:testdb

    driverClassName: org.h2.Driver


